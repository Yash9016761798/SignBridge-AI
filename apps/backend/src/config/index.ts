import appConfig from './app.config';
import databaseConfig from './database.config';
import firebaseConfig from './firebase.config';

const configs = [appConfig, databaseConfig, firebaseConfig];

export default configs;
